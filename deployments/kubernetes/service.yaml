apiVersion: v1
kind: Service
metadata:
  labels:
    app: sentiment-api
  name: sentiment-api-service
  namespace: development
spec:
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: sentiment-api
  type: ClusterIP
status:
  loadBalancer: {}
