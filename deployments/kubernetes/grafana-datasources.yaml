apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: development
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      url: http://sentiment-api-prometheus:9090
      access: proxy
      isDefault: true